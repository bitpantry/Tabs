@{
    Layout = "_BasicLayout";
}

<script>

    const redirectUrl = '@Model.RedirectUrl';
    const delay = @Model.Delay;

    setTimeout(function () {
        window.location.href = redirectUrl;
    }, delay);

</script>

